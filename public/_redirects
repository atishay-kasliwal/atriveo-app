# Landing page — served as static HTML, takes priority over SPA
/landing    /landing/index.html    200
/landing/   /landing/index.html    200

# API — pass through to Cloudflare Functions
/api/*      /api/:splat            200

# React SPA catch-all
/*          /index.html            200

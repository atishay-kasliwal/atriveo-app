node_modules
dist
.env
.env.tailor
scripts/cloudflared-tailor.yml
scripts/preview.html
.dev.vars
migration-output/

# Claude Code local settings (machine-specific)
.claude/

# Lovable reference export (local only — not part of the app build)
Dark Mode Magic/
Dark Mode Magic.zip
.DS_Store

# Resume engine (local only — binary PDFs, .tex, personal data)
resume-engine/

# Large binaries / generated files that should never be committed
*.dmg
*.pdf
"Atishay Kasliwal.pdf"
